import { createContext, useContext, useState } from "react";
export const ThemeStatus = createContext();

// const Theme = useContext(ThemeStatus);
// export function handleTheme() {
//   setTheme((prev) => !prev);
// }
